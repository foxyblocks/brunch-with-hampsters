{{#camelize}}{{name}}{{/camelize}}View = require 'views/{{name}}'

describe '{{#camelize}}{{name}}{{/camelize}}View', ->
  beforeEach ->
    @view = new {{#camelize}}{{name}}{{/camelize}}View()

  it 'PENDING tests something or gets deleted'
